{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "context-ledger": {
      "type": "stdio",
      "command": "./bin/context-ledger-mcp",
      "args": [
        "serve"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
